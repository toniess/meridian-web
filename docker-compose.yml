services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      API_BASE: ${API_BASE:-}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
      REVALIDATE_SECONDS: ${REVALIDATE_SECONDS:-60}
    restart: unless-stopped
